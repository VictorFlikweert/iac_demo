---
- name: Pull on panelpc then push to workers
  hosts: control
  gather_facts: false
  tasks:
    - name: Trigger post-pull worker update
      ansible.builtin.command:
        cmd: /workspace/scripts/post_pull.sh
      changed_when: false
